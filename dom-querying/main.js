const alpha = document.querySelector('h1');
console.log(alpha);

console.dir(alpha);

const beta = document.querySelector('#explanation');
console.log(beta);

console.dir(beta);

const gamma = document.querySelector('.hint');
console.log(gamma);

console.dir(gamma);

const delta = document.querySelectorAll('p');
console.log(delta);

console.dir(delta);

const epsilon = document.querySelectorAll('.example-link');
console.log(epsilon);
